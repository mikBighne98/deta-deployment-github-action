on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v3
      - id: foo
        uses: mikBighne98/deployment-status-test@v1
        with:
          req_false: "false"
          req_true: "true"
          req_not_described: "not_described"
